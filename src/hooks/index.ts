// Re-export data hooks from data-provider for backwards compatibility
export {
  useData,
  useProjectsData,
  useExperimentsData,
  useServicesData,
  useCertificationsData,
  useHeroStatsData,
  usePostsData,
  useCategoriesData,
  useContactSettings,
} from "@/components/providers/data-provider";
